namespace SimOverlay.App;

internal static class Program
{
    [STAThread]
    private static void Main()
    {
        // Entry point — host, tray icon, and overlay lifecycle will be wired here.
    }
}
